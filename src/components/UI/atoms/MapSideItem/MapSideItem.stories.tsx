import MapSideItem from "./MapSideItem";

export default {
  title: "Atom/Map Side Item",
  component: MapSideItem,
  argTypes: {},
};

const Template = () => <MapSideItem />;

export const defaultMapSideItem = Template.bind({});
