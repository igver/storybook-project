import MyAccordion from "./MyAccordion";

export default {
  title: "Organism/My accordion",
  component: MyAccordion,
  argTypes: {},
};

const Template = () => <MyAccordion />;

export const defaultMyAccordion = Template.bind({});
